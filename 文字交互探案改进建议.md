# detective-game 文字交互探案改进建议

面向对象：游戏策划、叙事/内容编辑、前端开发、交互设计、测试人员。

本文基于当前 `detective-game` 项目代码与案件数据进行评审，重点讨论如何把现有“案情阅读 + 选择题结案”的形态，升级为更有参与感、更可复玩、更利于内容扩展的文字互动探案游戏。

## 1. 总体判断

当前项目的优势很明确：

- 案件素材数量充足，已有 38 个案卷，覆盖伪装自杀、身份推理、路线时间、物证翻案、机关诡计、文字谜等多种题型。
- 每个案件都具备完整闭环：案情、读者注释、调查线索、轻/强提示、真相、推理过程、启发总结。
- 前端已有基础流程：阅卷、勘查、断案、提交、评分、复盘、进度保存。

但从“文字交互探案”的目标看，目前核心体验仍偏弱。玩家主要是在阅读作者整理好的材料，然后选择一个推理方向，勾选若干已解锁线索。这个流程更像“推理题库”或“案情学习卡片”，还不像真正的“玩家亲自办案”。

建议后续目标不要先追求复杂视觉表现，而是优先补齐推理游戏最关键的三件事：

1. 玩家需要做有代价的调查选择。
2. 玩家需要构建可验证的推理链。
3. 系统需要对玩家的错误推理给出具体反馈。

## 2. 当前主要不足

### 2.1 调查阶段没有选择压力

当前表现：

- `src/App.tsx` 中，玩家点击“调查此处”即可解锁一条线索。
- 所有调查动作默认并列展示。
- 调查顺序不会影响内容。
- 调查数量没有成本，也没有误查、漏查、追问、前置条件。

带来的问题：

- 玩家不是在判断“我该先查哪里”，而是在逐张翻开线索卡。
- 勘查阶段缺少悬念，因为所有关键材料最终都会被玩家低成本拿到。
- 案件之间的流程高度一致，玩几案后容易形成机械点击。

可落实改法：

- 把“调查动作”从固定五张卡，改成多个调查节点。
- 每个节点增加类型，例如 `scene`、`witness`、`evidence`、`timeline`、`questioning`。
- 给部分节点设置前置条件，只有发现某条线索后才能追问。
- 给调查设置轻量成本，例如每案最多初始调查 4 次，关键追问额外消耗 1 次。
- 允许存在“干扰调查”，它提供背景信息，但不直接支撑真相。

示例：

```ts
investigationNodes: [
  {
    id: "scene-stool",
    type: "scene",
    title: "查看倒地板凳",
    result: "板凳位置离死者双脚过远。",
    tags: ["关键线索", "现场矛盾"],
    unlocks: ["deduce-hanging-distance"]
  },
  {
    id: "witness-zhou",
    type: "witness",
    title: "盘问周二发现尸体后的动作",
    result: "周二一边说自己吓坏了，一边承认锄头是自己放好的。",
    requires: ["scene-hoe"],
    tags: ["关键线索", "口供矛盾"]
  }
]
```

### 2.2 断案阶段过于像选择题

当前表现：

- 玩家只需选择一个“最合理推理方向”。
- 再勾选至少 2 条已解锁线索。
- 最后一段文字说明是可选项。

带来的问题：

- 单选题过早暴露推理方向，玩家容易通过排除选项猜答案。
- “支撑证据”没有和具体结论绑定。
- 玩家可以选正确结论，但证据链是错的；也可以选错结论，却靠多勾线索拿分。

可落实改法：

把断案从“选择答案”改成“提交推理链”。至少拆成四类问题：

1. 真相判断：本案表面判断是否成立？
2. 关键矛盾：哪一个事实最能推翻表面判断？
3. 证据组合：哪些线索共同支持该矛盾？
4. 最终结论：凶手/动机/手法/真实目的是什么？

以“梁上白绫”为例：

- 表面判断：赵氏自尽。
- 关键矛盾：自尽动作与现场物证不匹配。
- 支撑证据：衣襟新破、周二抓痕、脚离板凳过远、锄头被整理。
- 最终结论：赵氏被周二勒死后伪装上吊。

界面上可以不做复杂输入，仍然用选择控件：

- 第一步：选择“表面判断是否可信”。
- 第二步：选择“最大矛盾点”。
- 第三步：勾选 2 到 4 条证据。
- 第四步：从若干结论中选择最终真相。
- 第五步：可选文字补充，用于加分和沉浸感。

### 2.3 评分系统没有识别关键证据

当前表现：

- `src/data/progress.ts` 的 `calculateScore()` 会把任意已存在的调查行动都当成有效证据。
- 只要 `selectedEvidenceIds` 属于本案调查行动，就能得证据分。
- `finalNote` 只要非空就加分。

带来的问题：

- 玩家乱勾线索也可能得分。
- 系统无法区分“关键证据”“辅助证据”“误导线索”。
- 评分参考与玩家实际推理之间缺少对应关系。

可落实改法：

在案件数据里明确标注证据价值：

```ts
evidence: [
  {
    id: "torn-clothes",
    title: "衣襟新破",
    content: "赵氏衣襟被撕开一道新口子。",
    role: "key",
    supports: ["not-suicide", "struggle-before-death"]
  },
  {
    id: "hoe-neat",
    title: "墙角锄头",
    content: "锄头立得端正，尖上还有湿泥。",
    role: "key",
    supports: ["scene-staged"]
  },
  {
    id: "neighbor-rumor",
    title: "邻人口角传闻",
    content: "邻居说二人近日争吵。",
    role: "supporting",
    supports: ["motive"]
  },
  {
    id: "old-rope",
    title: "旧绳磨损",
    content: "绳子看起来常年使用。",
    role: "decoy",
    misleadsTo: ["suicide"]
  }
]
```

评分建议：

- 真相方向正确：3 分。
- 关键矛盾选择正确：2 分。
- 每条关键证据：1 分，最多 3 分。
- 选择误导证据：每条扣 1 分。
- 推理说明命中关键词或非空但合理：1 到 2 分。
- 使用轻提示扣 1 分，强提示扣 2 分。

注意：如果暂时不做自然语言理解，不建议用 AI 自动判分。可以先通过结构化选择保证评分可靠。

### 2.4 案件数据过度模板化

当前表现：

- `src/data/cases.ts` 用 `actionsFrom()` 给每案生成固定调查标题：勘查现场、盘问证人、核对物证、复查时间与路线、追问反常点。
- `conclusionFor()` 按案件类型生成正确选项。
- `decoysFor()` 按案件类型生成干扰选项。

带来的问题：

- 不同案件的交互节奏太像。
- 玩家容易根据题型猜结论。
- 案件中的特殊物件、特殊人物、特殊时空关系没有充分进入玩法。

可落实改法：

每个案件应有专属调查结构，而不是仅有通用线索列表。

建议策划/内容编辑为每案补齐以下字段：

```ts
caseDesign: {
  surfaceStory: "看起来是什么案",
  realStory: "真实发生了什么",
  coreContradiction: "最关键的矛盾",
  playerFantasy: "玩家在本案中扮演什么判断角色",
  investigationFocus: ["现场重建", "证词矛盾", "物证用途"],
  redHerrings: ["最容易误导玩家的表象"],
  mustUnderstand: ["玩家破案必须理解的知识点"]
}
```

示例：

```ts
caseDesign: {
  surfaceStory: "赵氏上吊自尽",
  realStory: "赵氏被周二勒死后伪装上吊",
  coreContradiction: "自尽现场不该同时出现死前扭打和从容整理痕迹",
  playerFantasy: "像仵作和县令一样，从现场细节推翻报案人口供",
  investigationFocus: ["尸体姿态", "现场布置", "报案人口供"],
  redHerrings: ["白绫和倒地板凳让人先入为主地相信自尽"],
  mustUnderstand: ["自杀动作与现场物证必须互相匹配"]
}
```

### 2.5 复盘反馈不够针对玩家

当前表现：

- 结案后展示玩家选择、真相、标准推理、评分参考。
- 没有解释玩家选错的答案为什么错。
- 没有逐条说明玩家勾选的证据是否有效。

带来的问题：

- 玩家知道标准答案，但不知道自己具体错在哪里。
- 错误反馈缺少教学价值。
- 复玩动力不足。

可落实改法：

提交后生成“玩家推理诊断”：

```ts
feedbackRules: {
  truthOptions: {
    truth: "你抓住了本案核心：现场不是自然自尽，而是被整理过。",
    "decoy-1": "这个判断过于相信报案人口供，但没有解释赵氏衣物和周二抓痕。",
    "decoy-2": "外人闯入无法解释锄头被从容放好，也无法解释周二身上的新抓痕。"
  },
  evidence: {
    "torn-clothes": "有效。它说明赵氏死前可能挣扎过。",
    "neck-scratch": "有效。它能把赵氏的挣扎和周二联系起来。",
    "old-rope": "弱证据。绳子本身不能证明自杀或他杀。"
  }
}
```

复盘界面建议显示：

- 你的结论：正确/错误。
- 你的关键矛盾：命中/偏离。
- 你的证据：关键证据、辅助证据、误导证据分别列出。
- 你漏掉的关键点：最多提示 1 到 2 个。
- 标准推理链：按“事实 → 矛盾 → 推论 → 结论”展示。

## 3. 建议的新核心循环

推荐把一案的体验改为以下循环：

1. 阅卷：玩家获得表面案情和任务。
2. 初查：玩家选择 3 到 5 个调查节点。
3. 追问：根据已发现矛盾，解锁 1 到 3 个追问或复查节点。
4. 整理证据：玩家把线索归类到“事实”“矛盾”“推论”。
5. 断案：玩家提交结构化推理链。
6. 复盘：系统逐项反馈玩家推理质量。

这个循环仍然是纯文字为主，不需要大量美术、动画或复杂 AI。它的重点是让玩家感到：

- 我选择了调查方向。
- 我发现了前后矛盾。
- 我用证据支撑了结论。
- 我错的时候能知道错在哪里。

## 4. 数据结构改造建议

### 4.1 案件主结构

建议将 `DetectiveCase` 从“内容展示结构”升级为“可推理结构”：

```ts
type DetectiveCase = {
  id: string;
  title: string;
  subtitle: string;
  difficulty: Difficulty;
  category: CaseCategory;
  intro: string[];
  readerNote: string;
  playerTask: string;
  design: CaseDesign;
  investigationNodes: InvestigationNode[];
  deductions: DeductionStep[];
  solution: CaseSolution;
  feedback: CaseFeedback;
};
```

### 4.2 调查节点

```ts
type InvestigationNode = {
  id: string;
  type: "scene" | "witness" | "evidence" | "timeline" | "knowledge";
  title: string;
  prompt?: string;
  result: string;
  cost: number;
  requires?: string[];
  unlocks?: string[];
  evidenceIds?: string[];
};
```

### 4.3 证据

```ts
type Evidence = {
  id: string;
  title: string;
  content: string;
  role: "key" | "supporting" | "context" | "decoy";
  supports?: string[];
  misleadsTo?: string[];
};
```

### 4.4 推理步骤

```ts
type DeductionStep = {
  id: string;
  prompt: string;
  type: "single" | "multi" | "text";
  options?: DeductionOption[];
  correctOptionIds?: string[];
  requiredEvidenceIds?: string[];
};
```

### 4.5 反馈规则

```ts
type CaseFeedback = {
  optionFeedback: Record<string, string>;
  evidenceFeedback: Record<string, string>;
  missingKeyEvidence: Record<string, string>;
  finalSummary: {
    excellent: string;
    partial: string;
    failed: string;
  };
};
```

## 5. 策划与内容生产规范

每个案件进入开发前，策划/内容编辑应先完成一张“案件设计卡”。

### 5.1 案件设计卡模板

```md
## 案件标题

### 表面故事
玩家一开始会以为发生了什么？

### 真实故事
实际发生了什么？

### 核心矛盾
哪一个矛盾最能推翻表面故事？

### 关键证据
- 证据 A：支撑什么推论？
- 证据 B：支撑什么推论？
- 证据 C：支撑什么推论？

### 误导线索
- 误导 A：会把玩家引向什么错误判断？
- 误导 B：为什么它看似合理但其实不够？

### 调查节点
- 初始可查节点：
- 条件解锁节点：
- 追问节点：

### 标准推理链
事实 1 + 事实 2 → 矛盾 → 推论 → 结论

### 错误答案反馈
- 错误结论 A 为什么错？
- 错误结论 B 为什么错？
```

### 5.2 内容写作要求

每案至少应有：

- 1 个表面上很合理的错误判断。
- 1 个能推翻表面判断的核心矛盾。
- 3 条关键证据。
- 1 到 2 条辅助证据。
- 1 到 2 条误导线索。
- 2 个以上错误结论的专属反驳。

避免：

- 所有线索都直接指向真相。
- 只靠一个“神奇知识点”破案。
- 强提示直接说出答案。
- 复盘只给标准答案，不解释错误路径。

## 6. 交互设计建议

### 6.1 阅卷页

目标：让玩家形成初始假设。

建议增加：

- “你目前的初判是什么？”选择项。
- 初判不计分，只用于复盘时对比玩家是否被误导。
- 显示任务目标，例如“判断是否自杀”“找出真实第一现场”“判断证词是否可信”。

### 6.2 勘查页

目标：让玩家产生调查选择。

建议增加：

- 调查次数或行动点。
- 分类筛选：现场、人物、物证、时间线。
- 条件解锁：发现某矛盾后出现“追问”。
- 线索重要性不要提前暴露。

### 6.3 证据簿

目标：帮助玩家整理信息。

建议分成四栏：

- 已知事实。
- 可疑矛盾。
- 待解释问题。
- 我的推论。

第一版可以不用拖拽，用“加入证据簿”按钮即可。

### 6.4 断案页

目标：提交推理链，而不是猜答案。

建议结构：

1. 表面说法是否成立？
2. 最大矛盾是什么？
3. 哪些证据支持这个矛盾？
4. 最终真相是什么？
5. 一句话陈词。

### 6.5 复盘页

目标：让玩家知道自己为什么对或错。

建议结构：

- 总评：神断/可疑/误判。
- 结论判断：正确或错误。
- 关键矛盾：是否命中。
- 证据选择：关键、辅助、误导、遗漏。
- 标准推理链。
- 本案方法论总结。

## 7. 开发实施计划

### 阶段一：修正评分与证据模型

目标：不大改 UI，先让系统能识别证据质量。

开发任务：

- 在数据层增加 `Evidence` 概念。
- 给每条线索标注 `role`。
- 修改 `calculateScore()`，只给关键证据和辅助证据加分。
- 选择误导证据扣分。
- `finalNote` 暂时只做非空加分，但上限降低。

验收标准：

- 玩家只勾无关线索不能高分。
- 玩家选错真相但乱勾证据不能高分。
- 评分参考能显示每条证据的有效性。

### 阶段二：改造断案为结构化推理链

目标：让玩家提交“矛盾 + 证据 + 结论”。

开发任务：

- 增加 `DeductionStep` 数据结构。
- 断案页按步骤渲染问题。
- 支持单选、多选、短文本。
- 分步骤计算得分。
- 复盘页按步骤展示玩家答案。

验收标准：

- 每案至少有 3 个断案步骤。
- 玩家不能只靠最终单选拿满分。
- 错误步骤能在复盘里定位。

### 阶段三：增加条件调查与追问

目标：让调查阶段有决策感。

开发任务：

- 增加 `InvestigationNode.requires` 和 `unlocks`。
- 初始只展示部分节点。
- 解锁特定线索后出现追问节点。
- 可选增加调查行动点。

验收标准：

- 至少 3 个示范案件存在条件解锁。
- 玩家第一次进入勘查页不能直接看到所有关键线索。
- 追问节点能明显强化“发现矛盾”的感觉。

### 阶段四：优化复盘反馈

目标：让错误也有价值。

开发任务：

- 增加 `CaseFeedback`。
- 为错误结论配置专属反驳。
- 为证据配置逐条反馈。
- 复盘页展示遗漏关键证据。

验收标准：

- 玩家选错答案后能看到“为什么错”。
- 玩家选中误导证据后能看到“为什么它不足以定案”。
- 标准推理链按事实、矛盾、推论、结论展示。

## 8. 推荐先改造的三个案件

不建议一开始改 38 个案件。建议选 3 个代表类型做样板。

### 8.1 01 梁上白绫

适合作为入门样板。

原因：

- 物证清楚。
- 推理链短。
- 能很好展示“伪装自杀”的核心玩法。

重点改造：

- 加入“表面自杀”初判。
- 加入“死前扭打”和“现场整理”两个矛盾。
- 让玩家必须同时命中抓痕、衣襟、板凳距离、锄头位置中的关键组合。

### 8.2 02 假使团为何进京

适合作为动机排除样板。

原因：

- 核心不是物证，而是“如果目的只是 A，后续行为是否必要”。
- 可以训练玩家做目的链推理。

重点改造：

- 把三个动机拆成可比较的假设。
- 玩家需要逐一判断“挑起战火”“冒领赏赐”“潜入京城”能否解释全部行为。
- 复盘展示每种动机解释不了什么。

### 8.3 09 低垂的双手

适合作为进阶物证样板。

原因：

- 物证有强误导性。
- 血手印看似直接定罪，但位置和动作逻辑能推翻它。

重点改造：

- 让玩家先被“左手血印”吸引。
- 再通过双手低垂、灯灭、剪刀握法解锁反证。
- 复盘强调“物证不只看属于谁，还要看出现位置是否符合动作”。

## 9. 各角色分工建议

### 游戏策划

- 定义每类案件的核心玩法模板。
- 设计调查节点、行动点、解锁关系。
- 制定评分规则和难度曲线。

### 叙事/内容编辑

- 为每案补案件设计卡。
- 标注关键证据、辅助证据、误导线索。
- 编写错误答案反馈和标准推理链。

### 前端开发

- 改造数据结构和渲染组件。
- 实现结构化断案流程。
- 实现证据簿、复盘反馈、进度保存迁移。

### 交互设计

- 优化阅卷、勘查、断案、复盘的页面层级。
- 让玩家明确知道“我现在该做什么”。
- 避免把所有信息一次性平铺。

### 测试人员

- 测试每案是否能正常解锁、提交、复盘。
- 测试乱选、漏选、误选时评分是否合理。
- 测试进度保存与重开案件。

## 10. 风险与注意事项

### 10.1 不要过早引入开放式 AI 判案

开放式文本判分很诱人，但会带来稳定性、成本和可解释性问题。第一阶段建议用结构化选择完成核心体验。等案件数据稳定后，再考虑 AI 辅助点评。

### 10.2 不要把复杂度堆给玩家

调查节点、证据簿、推理链都要服务于“更清楚地推理”，而不是制造操作负担。入门案件应保持短流程，高阶案件再增加复杂度。

### 10.3 不要一次性重做全部案件

先做 3 个样板案，跑通数据结构、交互、评分和复盘，再批量迁移。否则 38 个案件一起改，内容返工成本会很高。

### 10.4 保留现有内容资产

当前案情、提示、真相和 takeaway 都有价值，不需要推倒重写。主要工作是把它们拆成更可交互的数据结构。

## 11. 建议的短期任务清单

第一周：

- 确定新数据结构。
- 选定 01、02、09 三个样板案。
- 为三个样板案补案件设计卡。

第二周：

- 实现证据 `role` 与新评分。
- 改造断案页为结构化步骤。
- 完成 01 案端到端流程。

第三周：

- 增加条件调查和追问节点。
- 完成 02、09 案样板。
- 增加复盘逐条反馈。

第四周：

- 内部试玩。
- 记录玩家误判点。
- 调整提示、评分和节点顺序。
- 决定是否批量迁移剩余案件。

## 12. 最小可交付版本定义

如果团队资源有限，建议把第一版目标定为：

- 3 个高质量样板案件。
- 每案 6 到 10 个调查节点。
- 每案至少 3 个结构化断案步骤。
- 每案至少 3 条关键证据、1 条误导证据。
- 复盘能解释玩家选错的结论和误导证据。
- 评分能区分关键证据、辅助证据、误导证据。

做到这一步，即使案件数量少于当前 38 个，整体体验也会比现在更像真正的文字互动探案游戏。

