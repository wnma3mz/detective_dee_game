# detective-game 文字交互探案改进建议

> 最后更新：2026-06-11。已核查代码中的 `actionPointLimit` 与 `docs/playtest-log.md` 基准值一致（01/02=6，04/07/09/13/25=5，21=7）；`npm run check` 通过。当前无新增可编程待办，等待 P1 人工试玩数据。

## 已完成

- 结构化数据模型：`Evidence`、`InvestigationNode`、`DeductionStep`、`CaseFeedback`。
- 8 个精制版样板：`01 梁上白绫`、`02 假使团为何进京`、`04 闭城后的车辙`、`07 客栈门栓`、`09 低垂的双手`、`13 夜来的假钦差`、`21 指南车下的吸铁石`、`25 三句遗言`。
- 行动点系统：`actionPointLimit` + `actionPointsSpent`，`remaining >= node.cost`。
- 证据倍率：`requiredEvidenceIds` 命中比例计算（`0.6 + 0.4 * hits/total`）。
- 提交条件：推理步骤全答 + 至少选择 2 条证据；阻断原因显示具体说明。
- 阅卷初判：3 选项 + 复盘"初判 vs 最终结论"对比。
- 轻量证据簿：勘查页归类"事实/矛盾/推论"，复盘展示归类质量，误导证据被归为推论时给出引用原文的专属警示。
- 阶段切换滚动：前往勘查/断案/提交结案后自动 `scrollIntoView`，案件切换仍回顶部。
- 工具链：`npm run check`（`validate:cases` + `build`），`tsx` 已入 devDependencies。
- 批量迁移准备：`docs/case-authoring-guide.md` + `scripts/case-template.ts` 已建立。
- P0 工具规范：指南已加入勘查禁用词、红鲱鱼链必填、中性选项规则；模板已去掉明显泄题占位，并加入红鲱鱼链注释。
- 本轮 P0 复查：第六轮新增断案选项长度差 lint（正确选项比最长错误选项多超过 6 字即 warning），并缩短 01/07/25 的 `step-conclusion` 正确选项；核心勘查与断案文本已从“明显泄题”降到“自动 lint 通过，需试玩验证”。
- P1 试玩记录模板：`docs/playtest-log.md` 已建立，覆盖 8 案两轮试玩、红鲱鱼链摘要、重点验证项和行动点调参汇总表。
- P1 单案调参：`21 指南车下的吸铁石` 已将 `scene-compass` 置于路线复盘之后，并新增薄雾、向导估算两个竞争解释节点；`actionPointLimit` 调为 7。
- 本轮 P1 复查：已核对 `caseExtensions` 与 `docs/playtest-log.md` 的行动点基准值一致，无需继续调整代码或模板。

---

## 待办

### P0：案件内容加厚，降低"看选项猜答案"

当前最大体验风险已从“勘查结果直接给结论 / 正确选项过长”下降为“自动 lint 通过但缺少真实试玩数据”。指南、模板、8 个样板案、文本 lint、长度差 lint 和试玩记录模板都已有基础改造，下一步应按 `docs/playtest-log.md` 填写真实试玩结果。

本轮复查仍发现的残留点：

- `21 指南车下的吸铁石`：磁石已后置，并增加薄雾、向导估算两条竞争解释；仍建议试玩时确认玩家是否会先被“自然迷路”链带偏，发现磁石后是否仍能完成完整推理链。
- 初判和复盘反馈里仍有“说明/不可能/证明”等解释性词汇；这类文案出现在复盘阶段可接受，但需要确保不会提前暴露到勘查前可见字段。
- `25 三句遗言` 的最终结论 label 已缩短为“趁地动劫饷，灭口知情者”，仍建议试玩时遮住勘查内容，只看选项验证是否仍能猜中。
- `docs/playtest-log.md` 目前是空模板，还没有真实试玩者、行动点剩余、最终得分率、常见误判点等数据；行动点基准值已核对无误。

本轮扫描结果判断：

- `InvestigationNode.result`、`Evidence.content`、`DeductionStep.option.label` 的高风险词已纳入 `validate-cases.ts` 文本 lint。
- `DeductionStep.option.label` 的长度差已纳入 lint，正确选项比最长错误选项多超过 6 字会 warning。
- `npm run check` 输出“文本 lint 通过，勘查前可见字段无高风险词”。
- 宽泛全文扫描仍会命中“各只有一道”“无法判断”和复盘 feedback 中的解释词，但这些不应作为 P0 阻塞；后续应以 lint、长度差检查和试玩为准。
- `docs/playtest-log.md` 的结构能承接 P1：每案两轮、红鲱鱼链、重点验证、行动点建议都有记录位置。
- 21 案本轮改造方向正确：先给路线偏差和薄雾/向导竞争解释，再通过路线复盘解锁指南车检查，能降低“一看到磁石就猜答案”的风险。
- 本轮没有发现新的自动化规则、模板或代码结构问题；后续判断应以真实试玩数据为准。

#### P0.1：勘查结果只写观察事实

禁止在 `result` 里出现"说明凶手""真正目标""不可能""只有一种解释""即栽赃"等结论性句式。只写可视化细节、位置、时间、数量、人物原话。如必须给解释，最多给两个互相竞争的解释，不指定正确项。

#### P0.2：断案选项改为中性短标签

选项只写结论标签（如"挑起边境战火"），理由全部移到 `feedback.optionFeedback`。验收：遮住勘查内容只看选项时，不能明显看出正确答案；正确选项不比错误选项长一大截。

#### P0.3：每案至少一条"完整但错误"的红鲱鱼链

每个精制案至少设计一条能自洽 60-70% 的错误推理链：
- 错误结论是什么
- 支持它的表面证据（至少 2 条）
- 它解释不了的关键矛盾（1 条）
- 复盘如何拆解（先承认为什么像真，再指出缺口）

#### P0.4：8 案逐案改造要点

- **01 梁上白绫**：尸身勘查改为具体距离/板凳倒向/脚尖位置；补"情绪低落""旧白绫""邻人听哭声"让自杀说更可信；选项改"自尽/他杀伪装/外人入室/暂不能定"。
- **02 假使团为何进京**：勘查不直说窑场是目标；增加贡礼/赏赐线索让财物动机成立；问题改"哪种动机能解释最多行为"。
- **04 闭城后的车辙**：先给车辙层次/城门时辰/人流事实，让玩家自己推时间窗口；时间选项改多个相近时段。
- **07 客栈门栓**：勘查只描述门栓/窗灰/声音，不直说"有人主动开门"；加旧墙裂缝/瓦片松动等误导入口。
- **09 低垂的双手**：血印描述改为外圈/内圈形态客观描述；强化孙喜旺不利行为；结论选项拆成"直接行凶/被迫参与/事后栽赃/外人巧合"。
- **13 夜来的假钦差**：让至少一条破绽有合理解释；加诏书格式正确/印信逼真等支撑"真钦差"的证据；增加"如何知道馆驿位置"第二层问题。
- **21 指南车下的吸铁石**：吸铁石后置，先给路线偏差/地图/向导证词；加雾/旧地图/临时换向导等备选解释；让玩家逐步排除。
- **25 三句遗言**：只展示三种句法组合，让玩家在断案里选哪种能解释全部事实；增加词义多义性；拆成词义选择→句法组合→推出灭口原因三步。

#### P0.5：新增 `InferenceLink` 类型（内容设计用，不一定立刻加 UI）

```ts
type InferenceLink = {
  id: string;
  evidenceIds: string[];       // 需要组合的证据
  conclusion: string;          // 推出的中间结论
  decoyConclusion?: string;    // 用相同证据可能误推出的错误结论
};
```

目的：强制内容设计时先想清楚"哪几条证据组合才能推出哪个中间结论"，避免单条证据直达最终结论。

#### P0.6：验收标准（改完后用 01/02/09 人工试玩）

- 只看断案选项，不能稳定猜中正确答案。
- 不看复盘，不能从勘查文本直接读到最终真相。
- 每案至少 1 条错误路径能让玩家认真犹豫。
- 组合至少 2 条证据才能推出关键中间结论。
- 复盘拆解"错误链为什么像真、哪里断了"，而不只是告知答案。
- `npm run check` 通过。

#### P0.7：同步修订指南与模板

已完成：

- `docs/case-authoring-guide.md`：已加入“不把答案写进勘查或选项”的核心原则、勘查禁用词、红鲱鱼链必填、中性选项规则。
- `scripts/case-template.ts`：已去掉“核心矛盾点（正确）”“真相：正确结论”“误判一”等泄题占位，改为中性示例，并加入红鲱鱼链注释占位。
- `scripts/validate-cases.ts`：已增加文本 lint，扫描 `Evidence.content`、非 knowledge 的 `InvestigationNode.result`、`DeductionStep.option.label` 中的高风险词；目前 warning 不 fail，但 `npm run check` 已显示 8 案无 warning。
- `scripts/validate-cases.ts`：已增加选项长度差 lint，正确选项比最长错误选项多超过 6 字时 warning；当前 8 案通过。
- `docs/playtest-log.md`：已增加 8 案试玩记录模板、每案红鲱鱼链摘要、重点验证项和 `actionPointLimit` 汇总调参表。
- `docs/playtest-log.md`：行动点汇总表已同步当前代码值，避免 P1 试玩调参时用旧基准。

仍需补充：

- 暂无新的工具规范阻塞；先完成 8 案各 2 轮人工试玩，再把反复出现的误判问题固化为 lint 或内容模板规则。

### P0：每轮 Review 必须包含模拟试玩路径

不能只跑 `npm run check`，至少走以下路径：

1. 重置进度 → 选 `01 梁上白绫` → 选错误初判
2. 点"前往勘查"，确认视口落到勘查面板
3. 调查 1 条误导线索 + 2-3 条关键线索
4. 给证据归类，把 1 条误导证据归为"推论"
5. 点"开始断案"，确认视口落到断案题目
6. 用不完整证据链提交
7. 确认优先看到得分与复盘，不是回顶部
8. 检查复盘是否解释初判偏差/证据不足/误导归类

进阶路径：用 `09 低垂的双手`，被"左手血印"误导，验证行动点压力和误导反馈。

### P1：8 案试玩调参

按 `docs/playtest-log.md` 对 8 个样板做 2 轮内部试玩，验证：行动点松紧、误导迷惑性、复盘解释质量、8 类案件手感差异、证据簿实际使用率。每案记录至少 3 个常见误判点和平均行动点剩余。

---

## 当前不建议做的事

- 不批量迁移 38 个案件（等 8 案稳定）。
- 不接入 AI 自由文本判案。
- 不重做视觉风格。
- 不提前暴露 `role: key/decoy` 标签给玩家。
- 不让证据簿归类参与评分。
- 不做拖拽排序。

<!-- [REVIEWED] 2026-06-11 已复查：代码 actionPointLimit 与 playtest-log 基准值一致（01/02=6，04/07/09/13/25=5，21=7），npm run check 通过；当前无新增可编程待办，等待 P1 人工试玩数据。 -->
