# detective-game 文字交互探案改进建议

> 2026-06-11 四轮更新：工具链已固定（npm run check）、行动点判断修正、证据倍率改为比例值、unlocks 语义定稿。待办见下方。

## 当前完成情况

- 结构化数据模型：`Evidence`、`InvestigationNode`、`DeductionStep`、`CaseFeedback`。
- 3 个精制版样板：`01 梁上白绫`、`02 假使团为何进京`、`09 低垂的双手`。
- 行动点系统：`actionPointLimit` + `actionPointsSpent`，节点按 `cost` 消耗，支持 `remaining >= node.cost`。
- 证据倍率：`requiredEvidenceIds` 命中率比例计算（`0.6 + 0.4 * hits/total`），复盘显示具体百分比。
- 提交条件：推理步骤全答 + 至少选择 2 条证据。
- 阅卷初判：三案各 3 个选项，复盘展示"阅卷初判 vs 最终结论"。
- 工具链：`npm run check`（串联 validate:cases + build），`tsx` 已加入 devDependencies。
- `types.ts` 注释：`requires` 是 UI 可见性唯一规则，`unlocks` 仅作内容设计标注。
- 数据校验：`npm run validate:cases` 通过，`npm run build` 通过。

## 待办

### P1：扩展到 8 个类型样板

现有 3 个样板覆盖：伪装自杀（01）、动机排除（02）、物证翻案（09）。

下一批补 5 个不同推理类型：

- `04 闭城后的车辙`：路线时间模板
- `07 客栈门栓`：密室现场模板
- `13 夜来的假钦差`：身份推理模板
- `21 指南车下的吸铁石`：机关诡计模板
- `25 三句遗言`：文字谜模板

每案必须包含：`evidenceList`、`investigationNodes`、`deductionSteps`、`feedback`、`actionPointLimit`、`initialHypotheses`，并通过 `npm run check`。

### P2：轻量证据簿

- 勘查页展示"已发现证据"面板（不暴露 key/decoy 标签）。
- 玩家可把证据归入：`事实`、`矛盾`、`推论` 三组。
- 第一版分组不参与评分，只用于沉浸和复盘。
- 复盘展示归类质量，误导证据被归为"推论核心"时给出专属提醒。

### 当前不建议做的事

- 不建议批量迁移 38 个案件，等 8 个样板稳定后再迁移。
- 不建议接入 AI 自由文本判案。
- 不建议重做视觉风格。
- 不建议把 `role: key/decoy` 提前暴露给玩家。

<!-- [READY_FOR_REVIEW] 2026-06-11 已完成：npm run check + tsx devDep + unlocks注释定稿 + 行动点remaining>=cost + 证据倍率比例化；同步清理历史归档压缩文件 -->
